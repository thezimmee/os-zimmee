#!/usr/bin/env bash

if [ -z $OSZ_PATH ]; then
	read -e -p "[??] Where is OS Zimmee installed? " -i "~/Projects/os-zimmee" OSZ_PATH
fi

# Symlink hammerspoon.
ln -sf "${OSZ_PATH}/hammerspoon/hammerspoon.link" ~/.hammerspoon
