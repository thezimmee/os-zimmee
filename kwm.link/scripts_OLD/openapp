// open app
app="$@"

if [[ $app == *"Google Chrome" ]]
then
	open -na "$app"
elif [[ $app == *"Sublime Text" ]]
then
	osascript /Users/thezimmee/applescripts/sublime_new.applescript
else
	open -a "$app"
fi
