#### COPIED FROM SAMPLE ####

# # Variables
# define hyper ctrl+alt
# define ~ /Users/thezimmee
# define layout ~/.kwm/scripts/layout
# define move ~/.kwm/scripts/move
# define pull ~/.kwm/scripts/pull
# define resizemode ~/.kwm/scripts/resizemode
# define openappdir ~/.kwm/scripts/openappdir
# define stubborn {Simplenote,Preview,TextEdit,Microsoft Word,MindNode,Snapper,Transmit,Byword,iA Writer,Steam,Ceceree,Mactracker}

# # Manage kwm
# kwmc bindsym hyper+shift+cmd-del quit
# kwmc bindsym hyper+shift+cmd-\ config reload
# kwmc bindsym hyper-e space -r focused
# kwmc bindsym hyper-k exec subl -n ~/.kwm

# # Launcher
# kwmc bindsym hyper-return exec openappdir Terminal
# kwmc bindsym hyper-t exec openappdir Sublime Text
# kwmc bindsym hyper-c exec openappdir Google Chrome
# kwmc bindsym hyper-o exec openappdir Finder
# kwmc bindsym hyper+shift-p exec osascript ~/Dropbox/Development/osx/applescripts/pomodoro.applescript
# kwmc bindsym hyper-z exec subl -n ~/.zshrc

# # Quit stubborn apps
# kwmc bindsym_passthrough cmd-w press cmd+alt-q stubborn -i

# # Tiling mode
# kwmc bindsym hyper-b space -t bsp
# kwmc bindsym hyper-f space -t monocle
# kwmc bindsym hyper-d space -t float

# # Toggle node mode
# kwmc bindsym hyper-v window -c type toggle

# # Rotate Window-Tree By 90* (Clockwise)
# kwmc bindsym hyper-r tree rotate 90

# # Resize modes
# kwmc bindsym hyper-s window -c split-mode toggle
# kwmc bindsym hyper-, window -c reduce 0.05
# kwmc bindsym hyper-. window -c expand 0.05

# kwmc mode resizenorth prefix off
# kwmc mode resizenorth color 0xFFC78EEB
# kwmc mode resizesouth prefix off
# kwmc mode resizesouth color 0xFFC78EEB
# kwmc mode resizeeast prefix off
# kwmc mode resizeeast color 0xFFC78EEB
# kwmc mode resizewest prefix off
# kwmc mode resizewest color 0xFFC78EEB

# kwmc bindsym cmd+ctrl-up exec resizemode north on
# kwmc bindsym cmd+ctrl-down exec resizemode south on
# kwmc bindsym cmd+ctrl-right exec resizemode east on
# kwmc bindsym cmd+ctrl-left exec resizemode west on

# kwmc bindsym resizenorth-escape exec resizemode north off
# kwmc bindsym resizesouth-escape exec resizemode south off
# kwmc bindsym resizeeast-escape exec resizemode east off
# kwmc bindsym resizewest-escape exec resizemode west off

# kwmc bindsym resizenorth-. window -c expand 0.05 north
# kwmc bindsym resizenorth-, window -c reduce 0.05 north
# kwmc bindsym resizesouth-. window -c expand 0.05 south
# kwmc bindsym resizesouth-, window -c reduce 0.05 south
# kwmc bindsym resizeeast-. window -c expand 0.05 east
# kwmc bindsym resizeeast-, window -c reduce 0.05 east
# kwmc bindsym resizewest-. window -c expand 0.05 west
# kwmc bindsym resizewest-, window -c reduce 0.05 west

# # Set zoom
# kwmc bindsym hyper-x window -z fullscreen
# kwmc bindsym hyper-p window -z parent
# kwmc bindsym hyper-w window -t focused

# # Mark Window
# kwmc bindsym hyper-m window -mk focused

# # Pseudo nodes
# kwmc bindsym hyper-y tree -pseudo create
# kwmc bindsym hyper+shift-y tree -pseudo destroy

# # Give Focus To Window
# kwmc bindsym hyper-left window -f west
# kwmc bindsym hyper-right window -f east
# kwmc bindsym hyper-down window -f south
# kwmc bindsym hyper-up window -f north

# # Focus next/previous monocle sub-tree
# kwmc bindsym cmd+ctrl-tab window -fm next
# kwmc bindsym cmd+ctrl+shift-tab window -fm prev

# # Swap Focused Window in direction
# kwmc bindsym hyper+shift-left window -s west
# kwmc bindsym hyper+shift-down window -s south
# kwmc bindsym hyper+shift-up window -s north
# kwmc bindsym hyper+shift-right window -s east

# # Move window to container in direction
# kwmc bindsym cmd+alt-up exec move north
# kwmc bindsym cmd+alt-right exec move east
# kwmc bindsym cmd+alt-down exec move south
# kwmc bindsym cmd+alt-left exec move west

# # Manage Layouts
# kwmc bindsym hyper+shift-l exec layout save
# kwmc bindsym hyper-l exec layout restore
# kwmc bindsym cmd+alt-2 tree restore 2up
# kwmc bindsym cmd+alt-3 tree restore 3up
# kwmc bindsym cmd+alt-4 tree restore 4up
# kwmc bindsym cmd+alt-5 tree restore 5up
# kwmc bindsym cmd+alt-6 tree restore 6up
# kwmc bindsym cmd+alt-7 tree restore 7up
# kwmc bindsym cmd+alt-8 tree restore quad

# # Move Focused window between spaces (don't follow)
# kwmc bindsym hyper+shift-tab window -m space previous
# kwmc bindsym hyper+shift-1 window -m space 1
# kwmc bindsym hyper+shift-2 window -m space 2
# kwmc bindsym hyper+shift-3 window -m space 3
# kwmc bindsym hyper+shift-4 window -m space 4
# kwmc bindsym hyper+shift-5 window -m space 5
# kwmc bindsym hyper+shift-6 window -m space 6
# kwmc bindsym hyper+shift-7 window -m space 7
# kwmc bindsym hyper+shift-8 window -m space 8
# kwmc bindsym hyper+shift-9 window -m space 9
# kwmc bindsym hyper+shift-0 window -m space 10

# # Move Focused window between spaces (follow)
# kwmc bindsym cmd+alt+shift-left exec pull left
# kwmc bindsym cmd+alt+shift-right exec pull right
# kwmc bindsym cmd+alt+shift-tab exec pull previous
# kwmc bindsym cmd+alt+shift-1 exec pull 1
# kwmc bindsym cmd+alt+shift-2 exec pull 2
# kwmc bindsym cmd+alt+shift-3 exec pull 3
# kwmc bindsym cmd+alt+shift-4 exec pull 4
# kwmc bindsym cmd+alt+shift-5 exec pull 5
# kwmc bindsym cmd+alt+shift-6 exec pull 6
# kwmc bindsym cmd+alt+shift-7 exec pull 7
# kwmc bindsym cmd+alt+shift-8 exec pull 8
# kwmc bindsym cmd+alt+shift-9 exec pull 9
# kwmc bindsym cmd+alt+shift-0 exec pull 0

# # Space switching
# kwmc bindsym hyper-tab space -f previous
# kwmc bindsym hyper-1 space -f 1
# kwmc bindsym hyper-2 space -f 2
# kwmc bindsym hyper-3 space -f 3
# kwmc bindsym hyper-4 space -f 4
# kwmc bindsym hyper-5 space -f 5
# kwmc bindsym hyper-6 space -f 6
# kwmc bindsym hyper-7 space -f 7
# kwmc bindsym hyper-8 space -f 8
# kwmc bindsym hyper-9 space -f 9
# kwmc bindsym hyper-0 space -f 0


#### MY OLD STUFF ####

# # resize window
# # define RESIZE_EAST BindingModeResizeEast
# # kwmc mode RESIZE_EAST prefix on
# # kwmc mode RESIZE_EAST timeout 0.75
# # kwmc mode RESIZE_EAST restore default
# # kwmc mode RESIZE_EAST color ddcc5577

# # kwmc bindsym ctrl+alt+cmd-l mode activate RESIZE_EAST

# # kwmc bindsym RESIZE_EAST-k window -c expand 0.025 east
# # kwmc bindsym RESIZE_EAST-j window -c reduce 0.025 east


# # kwmc bind ctrl+alt+cmd-h window -c reduce 0.025 west
# # kwmc bind ctrl+alt+cmd-j window -c expand 0.025 south
# # kwmc bind ctrl+alt+cmd-k window -c reduce 0.025 south
# # kwmc bind ctrl+alt+cmd-l window -c expand 0.025 west

# # kwmc bind ctrl+alt+cmd+shift-h window -c reduce 0.025 west
# # kwmc bind ctrl+alt+cmd+shift-j window -c reduce 0.025 south
# # kwmc bind ctrl+alt+cmd+shift-k window -c reduce 0.025 north
# # kwmc bind ctrl+alt+cmd+shift-l window -c reduce 0.025 east






########  Default Keybinds ########
# define MODE_MANAGER BindingModeSwitcher
# define YTD_MODE BindingModeYtd
# define SWAP_MODE BindingModeSwap
# define TREE_MODE BindingModeTree
# define SPACE_MODE BindingModeSpace

# kwmc mode MODE_MANAGER color ddbdd322
# kwmc mode MODE_MANAGER prefix on
# kwmc mode MODE_MANAGER timeout 2.0
# kwmc mode MODE_MANAGER restore default
# kwmc mode YTD_MODE color ddbdd322
# kwmc mode YTD_MODE prefix on
# kwmc mode YTD_MODE timeout 2.0
# kwmc mode YTD_MODE restore default
# kwmc mode SWAP_MODE color ddbdd322
# kwmc mode SWAP_MODE prefix on
# kwmc mode SWAP_MODE timeout 2.0
# kwmc mode SWAP_MODE restore default
# kwmc mode TREE_MODE color ddbdd322
# kwmc mode TREE_MODE prefix on
# kwmc mode TREE_MODE timeout 2.0
# kwmc mode TREE_MODE restore default
# kwmc mode SPACE_MODE color ddbdd322
# kwmc mode SPACE_MODE prefix on
# kwmc mode SPACE_MODE timeout 2.0
# kwmc mode SPACE_MODE restore default

# kwmc mode YTD_MODE color ddcc5577
# kwmc mode SWAP_MODE color dd458588
# kwmc mode TREE_MODE color ddfabd2f
# kwmc mode SPACE_MODE color ddb16286

# kwmc bindsym ctrl-f mode activate MODE_MANAGER
# kwmc bindsym ctrl-l mode activate YTD_MODE
# kwmc bindsym ctrl-h mode activate SWAP_MODE
# kwmc bindsym ctrl-j mode activate TREE_MODE
# kwmc bindsym ctrl-k mode activate SPACE_MODE

# kwmc bindsym MODE_MANAGER+ctrl-f mode activate default
# kwmc bindsym MODE_MANAGER-w mode activate YTD_MODE
# kwmc bindsym MODE_MANAGER-a mode activate SWAP_MODE
# kwmc bindsym MODE_MANAGER-s mode activate SPACE_MODE
# kwmc bindsym MODE_MANAGER-d mode activate TREE_MODE
# kwmc bindsym MODE_MANAGER-return exec terminal ; mode activate default

# kwmc bindsym YTD_MODE+ctrl-f mode activate default
# kwmc bindsym YTD_MODE-k window -c expand east
# kwmc bindsym YTD_MODE-j window -c reduce east

# kwmc bindsym SWAP_MODE+ctrl-f mode activate default
# kwmc bindsym SWAP_MODE-k window -c expand west
# kwmc bindsym SWAP_MODE-j window -c reduce west

# kwmc bindsym SPACE_MODE+ctrl-f mode activate default
# kwmc bindsym SPACE_MODE-k window -c expand north
# kwmc bindsym SPACE_MODE-j window -c reduce north

# kwmc bindsym TREE_MODE+ctrl-f mode activate default
# kwmc bindsym TREE_MODE-k window -c expand south
# kwmc bindsym TREE_MODE-j window -c reduce south





# Modify Container
# kwmc bind prefix-s window -c split-mode toggle
# kwmc bind prefix-h window -c reduce 0.025
# kwmc bind prefix-l window -c expand 0.025
# kwmc bind prefix-< window -c type toggle

# Set Temporary Window Container
# kwmc bind prefix-f window -z fullscreen
# kwmc bind prefix-d window -z parent
# kwmc bind prefix-w window -t focused

# Mark Window
# kwmc bind ctrl+alt+shift-m window -mk focused
# kwmc bind ctrl+alt+shift-h window -mk west wrap
# kwmc bind ctrl+alt+shift-l window -mk east wrap
# kwmc bind ctrl+alt+shift-j window -mk south wrap
# kwmc bind ctrl+alt+shift-k window -mk north wrap

# Give Focus To Window
# kwmc bind ctrl+alt-h window -f west
# kwmc bind ctrl+alt-l window -f east
# kwmc bind ctrl+alt-j window -f south
# kwmc bind ctrl+alt-k window -f north

# kwmc bind ctrl+alt-n window -fm prev
# kwmc bind ctrl+alt-m window -fm next

# Give Focus To Screen
# kwmc bind cmd+alt-1 display -f 0
# kwmc bind cmd+alt-2 display -f 1
# kwmc bind cmd+alt-3 display -f 2

# Swap Focused Window
# kwmc bind ctrl+alt+cmd-h window -s west
# kwmc bind ctrl+alt+cmd-j window -s south
# kwmc bind ctrl+alt+cmd-k window -s north
# kwmc bind ctrl+alt+cmd-l window -s east
# kwmc bind ctrl+alt+cmd-m window -s mark

# Pseudo containers
# kwmc bind cmd+ctrl+alt-p tree -pseudo create
# kwmc bind cmd+ctrl+alt-o tree -pseudo destroy
# kwmc bind ctrl+alt-p window -s prev
# kwmc bind ctrl+alt-n window -s next

# Detach Focused Window And Reinsert In Direction
# kwmc bind ctrl+shift-k window -m north
# kwmc bind ctrl+shift-l window -m east
# kwmc bind ctrl+shift-j window -m south
# kwmc bind ctrl+shift-h window -m west

# Detach Marked Window And Reinsert At Focused Window
# kwmc bind ctrl+shift-x window -m mark

# Move Focused Window To Space
# kwmc bind ctrl+alt-left window -m space left
# kwmc bind ctrl+alt-right window -m space right

# Move Focused Window To Screen
# kwmc bind ctrl+alt-1 window -m display 0
# kwmc bind ctrl+alt-2 window -m display 1
# kwmc bind ctrl+alt-3 window -m display 2

# Increase Container Gaps
# kwmc bind prefix-x space -g increase horizontal
# kwmc bind prefix-y space -g increase vertical

# Decrease Container Gaps
# kwmc bind prefix+shift-x space -g decrease horizontal
# kwmc bind prefix+shift-y space -g decrease vertical

# Increase Screen Padding
# kwmc bind prefix-left space -p increase left
# kwmc bind prefix-right space -p increase right
# kwmc bind prefix-up space -p increase top
# kwmc bind prefix-down space -p increase bottom
# kwmc bind prefix-p space -p increase all

# Decrease Screen Padding
# kwmc bind prefix+shift-left space -p decrease left
# kwmc bind prefix+shift-right space -p decrease right
# kwmc bind prefix+shift-up space -p decrease top
# kwmc bind prefix+shift-down space -p decrease bottom
# kwmc bind prefix+shift-p space -p decrease all

# Autowrite
# kwmc bind cmd-7 write \
# kwmc bind cmd-8 write {
# kwmc bind cmd-9 write }
