#!/usr/bin/env bash

if [ -z $OSZ_PATH ]; then
	read -e -p "[??] Where is OS Zimmee installed? " -i "~/Projects/os-zimmee" OSZ_PATH
fi

# Symlink karabiner-elements.
mkdir -p ~/.config && ln -sf "${OSZ_PATH}/karabiner-elements/karabiner.link" ~/.config/karabiner
